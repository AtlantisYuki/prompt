interface:
  display_name: "Software Development Process"
  short_description: "文档先行、确定性状态、Grill 评审与关闭门禁的 SDLC"
  default_prompt: "Use $software-dev-process to advance or close this task with deterministic task state, document review, verification, AI registration, and knowledge gates."

policy:
  allow_implicit_invocation: true
